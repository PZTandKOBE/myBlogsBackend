<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.pztsblog.blogsend.mapper.BlogMusicMapper">

    <select id="selectMusicList" resultType="org.pztsblog.blogsend.entity.BlogMusic">
        SELECT * FROM blog_music WHERE is_active = 1
    </select>

</mapper>